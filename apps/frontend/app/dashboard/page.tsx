'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';

type Role = 'APPLICANT' | 'TRAINEE' | 'REGISTRAR' | 'FINANCE_OFFICER' | 'DEPARTMENT_HEAD' | 'ADMIN' | 'SYSTEM_AUDITOR';
const cards: Record<Role, { title: string; href: string; description: string }[]> = { APPLICANT: [{ title: 'My application', href: '/apply', description: 'Complete your application and upload documents.' }], TRAINEE: [{ title: 'Academic profile', href: '/finance-academics', description: 'View courses, grades, attendance, and fees.' }], REGISTRAR: [{ title: 'Admissions', href: '/registrar', description: 'Review applications and manage placement.' }, { title: 'Students', href: '/student-registration', description: 'Register admitted trainees.' }], FINANCE_OFFICER: [{ title: 'Finance', href: '/finance-academics', description: 'Record payments and review balances.' }], DEPARTMENT_HEAD: [{ title: 'Academics', href: '/finance-academics', description: 'Manage courses and attendance.' }], ADMIN: [{ title: 'Administration', href: '/admin', description: 'Manage policies, users, and reports.' }], SYSTEM_AUDITOR: [{ title: 'Audit and reports', href: '/admin', description: 'Review operational reports.' }] };
export default function DashboardPage() { const router = useRouter(); const [role, setRole] = useState<Role | null>(null); useEffect(() => { const raw = localStorage.getItem('user'); if (!raw) return router.replace('/login'); setRole(JSON.parse(raw).role); }, [router]); if (!role) return <main className="grid min-h-screen place-items-center">Loading dashboard…</main>; return <main className="min-h-screen bg-slate-100 p-4 sm:p-8"><div className="mx-auto max-w-6xl"><header className="rounded-3xl bg-gradient-to-r from-brand-700 to-brand-500 p-8 text-white shadow-xl"><p className="text-xs font-bold uppercase tracking-[0.2em] text-teal-100">Kibet Construction and Industrial College</p><div className="mt-3 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-black">{role.replaceAll('_', ' ')} workspace</h1><p className="mt-2 text-teal-50">Choose an operation to continue.</p></div><button onClick={() => { localStorage.clear(); router.replace('/login'); }} className="rounded-xl bg-white/15 px-4 py-2 text-sm font-bold hover:bg-white/25">Sign out</button></div></header><section className="mt-6 grid gap-5 md:grid-cols-2">{cards[role].map((card) => <a key={card.href} href={card.href} className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 transition hover:-translate-y-1 hover:shadow-lg"><h2 className="text-xl font-black text-slate-900">{card.title}</h2><p className="mt-2 text-slate-600">{card.description}</p><span className="mt-5 inline-block font-bold text-brand-600">Open workspace →</span></a>)}</section></div></main>; }
